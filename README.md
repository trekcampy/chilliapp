This project is a React based front-end portion of chilli unit convertor that maps units of one chilli type to another chilli, based on their Scovillie value. Django server running on the same machine exports a Rest API that is used to get list of Chilli types and their Scoville values. Server can be found chillibackend repo.

In order to start the React server, clone the repo and run:

### `npm start`
